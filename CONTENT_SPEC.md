# 教材データ作成仕様書（必読・厳守）

Go学習Webサービス「Go 200 Steps」の教材データファイルを作成するための仕様。

## ファイル形式

各章は `public/content/chapterNN.js`（NNは2桁ゼロ埋め）として作成する。
ファイル全体は次の形式の**1つの関数呼び出しのみ**で構成する：

```js
// 第N章：章タイトル
registerChapter({
  number: N,
  title: "章タイトル",
  description: "章の概要を1〜2文で。",
  steps: [
    {
      id: 1,
      title: "ステップタイトル",
      explanation: `HTML解説（後述）`,
      task: `課題の指示。1〜3文。HTML可（<code>など）。`,
      code: `初期コード`,
      solution: `模範解答コード`,
      hints: [`ヒント1`, `ヒント2`],
      expectedOutput: "判定用文字列",
      requiredCode: [{ text: "%v", message: "不足時に表示する案内" }] // 任意
    }
    // ... 1章につき必ず10ステップ
  ]
});
```

## 絶対的な構文ルール

1. テンプレートリテラル内では **バッククォート記号を絶対に使わない**（必要なら \` とエスケープ。ただし原則使わない構成にする）
2. テンプレートリテラル内では **`${` を絶対に書かない**（Goのfmt.Printfは `%v` などなので通常出現しない。$の直後に{を書く必要がある場合は \${ とする）
3. ファイルは作成後に必ず `node --check ファイル名` で構文チェックし、エラーがあれば修正すること
4. `id` は全体の通し番号（第N章はステップ (N-1)*10+1 〜 N*10）

## explanation（解説）のルール

- **HTMLで記述**する（Markdown不可）。使ってよいタグ：`<p> <ul> <ol> <li> <table> <tr> <th> <td> <strong> <em> <code> <pre> <h4>`
- 分量の目安：日本語400〜800字＋コード例1〜2個。初心者が読んで理解でき、ミドルエンジニアにも学びがある深さにする
- コード例は `<pre><code>...</code></pre>`、インラインは `<code>...</code>`
- **HTML内のコード例では `<` を `&lt;` に、`&` を `&amp;` にエスケープすること**（例：`[]int` はそのままでよいが、`ch &lt;- v`、`&amp;user`、`map[string]int` 内の `&lt;` `&amp;` は必ずエスケープ）。これを忘れると表示が壊れるので最重要
- 用語の初出時は必ず短い説明を添える（例：「ゼロ値（変数宣言時に自動で入る初期値）」）
- 比較や一覧は`<table>`を積極的に使い視覚的に分かりやすくする
- 日本語の文章として自然に。日本語と英単語の間に不要な半角スペースを入れない。絵文字は使わない

## code（初期コード）のルール

- 学習者に「1から書かせる」のではなく、**例コードの部分修正・穴埋め・意図的なエラーの修正**を中心にする
  - パターンA：動くコードの一部を `// TODO: ここを〜に書き換える` で修正させる
  - パターンB：意図的にコンパイルエラーになるコードを直させる（未使用変数・型不一致・nilマップへの代入など。エラーメッセージを読む訓練になる）
  - パターンC：完成例をそのまま実行して動作を観察し、数値や文字列を変えて再実行させる（新概念の導入直後）
- 必ず `package main` と `func main()` を含む単一ファイルで完結すること
- 外部パッケージ使用禁止（標準ライブラリのみ）。**標準入力・ファイル入出力・ネットワークは使用禁止**。time.Sleepは50ms以下なら可
- コード内コメントは日本語

## solution（模範解答）のルール

- **必ずそのままコンパイルが通り、実行が成功する完全なコード**であること（go1.22想定）
- Goは未使用変数・未使用importがコンパイルエラーになるので特に注意する（意図的なエラー教材を除く）
- 作成した全ステップのsolutionを机上で必ず検算すること（型・所有権・出力内容）

## expectedOutput（自動判定）のルール

- solutionを実行したときの標準出力に**必ず含まれる特徴的な1行程度の文字列**を指定する
- 出力順序が非決定的な場合（スレッド並行実行など）は `null` にする
- mapのイテレーション順に依存する出力は判定に使わない（順序不定のため。キーをソートして出力させるか、特定キーの値のみ判定する）。goroutineの出力順にも依存しない

## requiredCode（使用コードの判定）のルール（任意）

- 出力の一致だけでは判定できない課題で使う（例：Printfを使わせたいのにPrintlnでも同じ出力になる、%vを使わせたいのに固定文字列で通ってしまう）
- 文字列、または `{ text, message }` オブジェクトの配列。`text` は**コメントを除いたコード**に含まれていなければならない部分文字列、`message` は不足しているときに学習者へ表示する案内（省略可）
- 文字列リテラルの中も検索対象になるので、`"%v"` のような書式指定子を要求できる。コメントは除外されるため、TODOコメントに書いた説明は判定に影響しない
- `solution` は必ずすべての `text` を含むこと。初期コード `code` には含まれていないこと（`node scripts/validate.js` が検証する）
- 判定は「出力が一致」→「requiredCodeを満たす」の順で行われ、両方を満たしたときだけクリアになる

## hints のルール

- 2〜3個。1つ目は考え方、2つ目以降は具体的な書き方に近づける（答えそのものは書かない）

## カリキュラム上の制約

- 各ステップは**それ以前のステップで学んだ知識のみ**を前提にする（先の章の概念を使わない）
- 章の最後の1〜2ステップはその章の総合演習にする
- ステップ間で難易度をなだらかに上げる
